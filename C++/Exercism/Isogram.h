#if !defined(ISOGRAM_H)
#define ISOGRAM_H
#include <algorithm>
#include <string>
#include <map>

using namespace std;

namespace isogram {

    bool is_isogram(const string &);
    
}  // 

#endif // ISOGRAM_H